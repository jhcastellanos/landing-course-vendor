# System Flow Diagrams

## 📊 Complete Purchase Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CUSTOMER JOURNEY                             │
└─────────────────────────────────────────────────────────────────────┘

1. BROWSE COURSES
   ┌──────────────┐
   │   Customer   │──► Opens http://localhost:3000
   └──────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  Next.js Frontend    │──► GET /api/courses
   └──────────────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  .NET Backend API    │──► Query Neon Database
   └──────────────────────┘
          │
          ▼
   ┌──────────────────────┐
   │   PostgreSQL         │──► Return 4 courses
   │   (Neon)             │
   └──────────────────────┘
          │
          ▼
   [Display course cards with images, prices, discounts]


2. SELECT COURSE
   ┌──────────────┐
   │   Customer   │──► Clicks "Buy Course" button
   └──────────────┘
          │
          ▼
   [Checkout Modal Opens]
   - Course name displayed
   - Price shown
   - Email input field
   - Name input field (optional)


3. ENTER INFORMATION
   ┌──────────────┐
   │   Customer   │──► Enters email: customer@example.com
   └──────────────┘    ──► Enters name: John Doe (optional)
          │
          ▼
   [PayPal Button Appears]


4. INITIATE PAYMENT
   ┌──────────────┐
   │   Customer   │──► Clicks PayPal button
   └──────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  Frontend            │──► POST /api/payment/create-order
   │                      │    {
   │                      │      courseId: 1,
   │                      │      customerEmail: "customer@example.com",
   │                      │      customerName: "John Doe"
   │                      │    }
   └──────────────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  Backend API         │──► 1. Get course details
   │  PaymentController   │    2. Create PayPal order
   │                      │    3. Create pending purchase in DB
   └──────────────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  PayPal API          │──► Create order
   │                      │    Return: orderId + approvalUrl
   └──────────────────────┘
          │
          ▼
   [Customer redirected to PayPal.com]


5. COMPLETE PAYMENT
   ┌──────────────┐
   │   Customer   │──► At PayPal.com
   └──────────────┘    ──► Logs in with PayPal account
          │                OR
          │            ──► Pays with credit card (Visa/Mastercard)
          ▼
   [Approves payment]
          │
          ▼
   [Redirected back to your site]


6. CAPTURE PAYMENT
   ┌──────────────────────┐
   │  Frontend            │──► POST /api/payment/capture-order
   │                      │    { orderId: "7XX12345..." }
   └──────────────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  Backend API         │──► 1. Capture PayPal payment
   │  PaymentController   │    2. Update purchase status
   │                      │    3. Generate purchase code
   │                      │    4. Trigger email sending
   └──────────────────────┘
          │
          ├──────────────────────┐
          │                      │
          ▼                      ▼
   ┌──────────────┐      ┌──────────────┐
   │  SendGrid    │      │  PostgreSQL  │
   │  Email API   │      │  Database    │
   └──────────────┘      └──────────────┘
          │                      │
          ▼                      ▼
   [2 Emails Sent]      [Purchase Saved]
   - Customer               - Customer record
   - Owner                  - Purchase record
                           - Unique code: CRS-ABC123XYZ456


7. CONFIRMATION
   ┌──────────────────────┐
   │  Success Screen      │──► Shows purchase code
   │                      │    "CRS-ABC123XYZ456"
   └──────────────────────┘
          │
          ▼
   ┌──────────────┐
   │   Customer   │──► Receives confirmation email
   └──────────────┘    ──► Sees purchase code

   ┌──────────────┐
   │    Owner     │──► Receives notification email
   └──────────────┘    ──► Sees customer details
```

---

## 🔄 Technical Flow Diagram

```
┌────────────────────────────────────────────────────────────┐
│                    TECHNICAL ARCHITECTURE                   │
└────────────────────────────────────────────────────────────┘

                        Browser (Customer)
                              │
                              │ HTTP Request
                              ▼
            ┌──────────────────────────────────┐
            │      FRONTEND (Next.js)          │
            │      Port 3000                   │
            ├──────────────────────────────────┤
            │  • React Components              │
            │    - HomePage                    │
            │    - CourseCard                  │
            │    - CheckoutModal               │
            │  • PayPal SDK Integration        │
            │  • API Client (axios)            │
            │  • TailwindCSS                   │
            └──────────────────────────────────┘
                              │
                              │ REST API Calls
                              │ (JSON over HTTP)
                              ▼
            ┌──────────────────────────────────┐
            │      BACKEND (.NET Core)         │
            │      Port 5000                   │
            ├──────────────────────────────────┤
            │  Controllers                     │
            │  ├─ CoursesController            │
            │  │   • GET /api/courses          │
            │  │   • GET /api/courses/{id}     │
            │  └─ PaymentController            │
            │      • POST /api/payment/        │
            │        create-order               │
            │      • POST /api/payment/        │
            │        capture-order              │
            ├──────────────────────────────────┤
            │  Services                        │
            │  ├─ PayPalService                │
            │  │   • CreateOrderAsync()        │
            │  │   • CaptureOrderAsync()       │
            │  ├─ SendGridEmailService         │
            │  │   • SendCustomerEmail()       │
            │  │   • SendOwnerEmail()          │
            │  └─ PurchaseService              │
            │      • CreatePurchaseAsync()     │
            │      • CompletePurchaseAsync()   │
            │      • GeneratePurchaseCode()    │
            ├──────────────────────────────────┤
            │  Data Layer                      │
            │  ├─ AppDbContext (EF Core)       │
            │  └─ Models                       │
            │      • Course                    │
            │      • Customer                  │
            │      • Purchase                  │
            └──────────────────────────────────┘
                    │              │
        ┌───────────┼──────────────┼───────────┐
        │           │              │           │
        ▼           ▼              ▼           ▼
┌──────────┐ ┌──────────┐  ┌──────────┐ ┌──────────┐
│ PayPal   │ │SendGrid  │  │  Neon    │ │  Neon    │
│   API    │ │Email API │  │PostgreSQL│ │PostgreSQL│
├──────────┤ ├──────────┤  ├──────────┤ ├──────────┤
│ Create   │ │Customer  │  │ courses  │ │customers │
│ Order    │ │Email     │  │          │ │          │
│          │ │          │  ├──────────┤ ├──────────┤
│ Capture  │ │Owner     │  │purchases │ │          │
│ Payment  │ │Email     │  │          │ │          │
└──────────┘ └──────────┘  └──────────┘ └──────────┘
```

---

## 📧 Email Flow

```
┌─────────────────────────────────────────────────────┐
│              EMAIL NOTIFICATION FLOW                 │
└─────────────────────────────────────────────────────┘

After successful payment capture:

┌──────────────────────┐
│  PurchaseService     │
│  CompletePurchaseAsync()
└──────────────────────┘
          │
          │ 1. Update purchase status to COMPLETED
          │ 2. Save transaction ID
          │ 3. Mark email_sent = true
          │
          ├─────────────────┬─────────────────┐
          │                 │                 │
          ▼                 ▼                 ▼
  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │ Save to DB   │  │Customer Email│  │ Owner Email  │
  └──────────────┘  └──────────────┘  └──────────────┘
                            │                 │
                            ▼                 ▼
                    ┌──────────────────────────────┐
                    │   SendGrid API               │
                    └──────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              │                           │
              ▼                           ▼
    ┌──────────────────┐        ┌──────────────────┐
    │ Customer Inbox   │        │  Owner Inbox     │
    ├──────────────────┤        ├──────────────────┤
    │ Subject:         │        │ Subject:         │
    │ "Course Purchase │        │ "New Course      │
    │  Confirmation"   │        │  Purchase"       │
    │                  │        │                  │
    │ Body:            │        │ Body:            │
    │ • Thank you msg  │        │ • Customer email │
    │ • Course name    │        │ • Course name    │
    │ • Purchase code  │        │ • Purchase code  │
    │   CRS-XXXXXXXXX  │        │ • Timestamp      │
    │ • HTML formatted │        │                  │
    └──────────────────┘        └──────────────────┘
```

---

## 🗄️ Database Relationships

```
┌─────────────────────────────────────────────────┐
│            DATABASE SCHEMA (Neon)                │
└─────────────────────────────────────────────────┘

┌────────────────────────┐
│       courses          │
├────────────────────────┤
│ id (PK)                │◄────────┐
│ name                   │         │
│ title                  │         │
│ description            │         │
│ image_url              │         │
│ full_price             │         │
│ discount_percentage    │         │
│ final_price            │         │  1:N
│ is_active              │         │
│ created_at             │         │
│ updated_at             │         │
└────────────────────────┘         │
                                   │
                                   │
┌────────────────────────┐         │
│      customers         │         │
├────────────────────────┤         │
│ id (PK)                │◄───┐    │
│ email (UNIQUE)         │    │    │
│ full_name              │    │    │
│ created_at             │    │    │
└────────────────────────┘    │    │
                              │    │
                              │ 1:N│
                              │    │
┌────────────────────────┐    │    │
│      purchases         │    │    │
├────────────────────────┤    │    │
│ id (PK)                │    │    │
│ customer_id (FK)       │────┘    │
│ course_id (FK)         │─────────┘
│ purchase_code (UNIQUE) │
│ amount_paid            │
│ payment_method         │
│ paypal_transaction_id  │
│ payment_status         │
│ purchased_at           │
│ email_sent             │
└────────────────────────┘

Example Data:

Customer: customer@example.com
  └─► Purchase 1: Course "Web Dev", Code: CRS-ABC123456789
  └─► Purchase 2: Course "Python", Code: CRS-XYZ987654321
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────┐
│              SECURITY MEASURES                   │
└─────────────────────────────────────────────────┘

1. ENVIRONMENT VARIABLES
   ┌──────────────────┐
   │ Local Dev        │──► .env.local (Frontend)
   │                  │    appsettings.json (Backend)
   └──────────────────┘
          │
          ▼
   Never committed to Git
   Secrets stored securely


2. CORS PROTECTION
   ┌──────────────────┐
   │ Backend API      │──► Only allows requests from:
   │                  │    • http://localhost:3000 (dev)
   │                  │    • https://yoursite.com (prod)
   └──────────────────┘


3. HTTPS (Production)
   ┌──────────────────┐
   │ All Traffic      │──► Encrypted with SSL/TLS
   │                  │    • Vercel: Auto HTTPS
   │                  │    • Render: Auto HTTPS
   └──────────────────┘


4. PAYMENT SECURITY
   ┌──────────────────┐
   │ PayPal           │──► • No card data stored
   │                  │    • PCI compliant
   │                  │    • Secure checkout
   └──────────────────┘


5. DATABASE
   ┌──────────────────┐
   │ Neon PostgreSQL  │──► • SSL required
   │                  │    • Password protected
   │                  │    • EF Core prevents SQL injection
   └──────────────────┘


6. INPUT VALIDATION
   ┌──────────────────┐
   │ Frontend         │──► Email validation
   └──────────────────┘
          │
          ▼
   ┌──────────────────┐
   │ Backend          │──► Model validation
   │                  │    Data annotations
   └──────────────────┘
```

---

## 📱 Responsive Design Flow

```
┌─────────────────────────────────────────────────┐
│           RESPONSIVE BREAKPOINTS                 │
└─────────────────────────────────────────────────┘

Mobile (375px - 640px)
┌────────┐
│ Course │
│  Card  │
├────────┤
│ Course │
│  Card  │
├────────┤
│ Course │
│  Card  │
└────────┘
1 column


Tablet (640px - 1024px)
┌────────┬────────┐
│ Course │ Course │
│  Card  │  Card  │
├────────┼────────┤
│ Course │ Course │
│  Card  │  Card  │
└────────┴────────┘
2 columns


Laptop (1024px - 1280px)
┌────────┬────────┬────────┐
│ Course │ Course │ Course │
│  Card  │  Card  │  Card  │
├────────┼────────┼────────┤
│ Course │        │        │
│  Card  │        │        │
└────────┴────────┴────────┘
3 columns


Desktop (1280px+)
┌────────┬────────┬────────┬────────┐
│ Course │ Course │ Course │ Course │
│  Card  │  Card  │  Card  │  Card  │
└────────┴────────┴────────┴────────┘
4 columns
```

---

## 🚀 Deployment Flow

```
┌─────────────────────────────────────────────────┐
│          DEPLOYMENT ARCHITECTURE                 │
└─────────────────────────────────────────────────┘

Development:
  localhost:3000 ──► localhost:5000 ──► Neon
  (Next.js)          (.NET API)         (PostgreSQL)


Production:
┌──────────────────────┐
│   Customer Browser   │
└──────────────────────┘
          │
          ▼
┌──────────────────────┐
│   Vercel CDN         │──► Frontend (Next.js)
│   your-app.vercel.app│    • Auto HTTPS
└──────────────────────┘    • Auto scaling
          │                 • Global CDN
          │ HTTPS
          ▼
┌──────────────────────┐
│   Render/Railway     │──► Backend (.NET API)
│   api.onrender.com   │    • Auto HTTPS
└──────────────────────┘    • Health checks
          │                 • Auto deploy
          │
    ┌─────┴─────┬─────────────┐
    │           │             │
    ▼           ▼             ▼
┌────────┐ ┌────────┐  ┌──────────┐
│ Neon   │ │PayPal  │  │SendGrid  │
│Database│ │ API    │  │Email API │
└────────┘ └────────┘  └──────────┘

All connections encrypted with SSL/TLS
```

---

This visual guide should help you understand how all the pieces fit together!
